<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_bottom_sheet_rounded">

    <!-- Handle bar -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@color/icon_inactive" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/wallet_create_title"
        android:textColor="@color/text_primary"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/wallet_create_desc"
        android:textColor="@color/icon_inactive"
        android:textSize="14sp"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="20dp" />

    <!-- Seed Phrase Grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvSeedWords"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:overScrollMode="never" />

    <!-- Copy button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCopySeed"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="Salin Frasa Pemulihan"
        android:textColor="@color/wallet_accent"
        android:textSize="14sp"
        android:textAllCaps="false"
        app:strokeColor="@color/wallet_accent"
        app:strokeWidth="1dp"
        app:cornerRadius="12dp"
        app:icon="@drawable/ic_copy"
        app:iconTint="@color/wallet_accent"
        app:iconGravity="textStart"
        android:layout_marginBottom="12dp" />

    <!-- Confirm button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnConfirmSaved"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/wallet_saved_confirm"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textAllCaps="false"
        android:textStyle="bold"
        android:backgroundTint="@color/wallet_accent"
        app:cornerRadius="16dp"
        android:layout_marginBottom="16dp" />

</LinearLayout>
